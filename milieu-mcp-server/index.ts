#!/usr/bin/env node

// TODO: Implement MCP server using @modelcontextprotocol/sdk
//
// Tool: scan_surface
// Input: { url: string, timeout?: number, explain?: boolean }
// Output: ScanResult (from milieu-cli's scan() function)
//
// Install command: npx -y @milieu/mcp-server
//
// Constraints:
// - No state, no caching — pure function wrapper around scan()
// - Inherits all security constraints from scan()
// - Uses @modelcontextprotocol/sdk for MCP server framework

console.error(
  "@milieu/mcp-server is not yet implemented. See https://github.com/simplyarun/milieu-cli for updates.",
);
process.exit(1);
