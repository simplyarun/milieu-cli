---
phase: 10-packaging-and-publishing
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - package.json
  - README.md
autonomous: false
requirements:
  - FOUND-01
  - PKG-01
  - PKG-03

must_haves:
  truths:
    - "npx milieu-cli scan <url> works from a local tarball (simulating first-time user)"
    - "Package metadata is complete for npmjs.com listing"
    - "README describes the current 5-bridge architecture and correct CLI syntax"
    - "Exactly 3 runtime dependencies with no additions"
  artifacts:
    - path: "package.json"
      provides: "Complete npm metadata and milieu-cli bin alias"
      contains: "milieu-cli"
    - path: "README.md"
      provides: "Accurate package documentation for npmjs.com"
      contains: "milieu scan"
  key_links:
    - from: "package.json bin.milieu-cli"
      to: "dist/cli/index.js"
      via: "bin alias for npx resolution"
      pattern: "milieu-cli.*dist/cli/index\\.js"
    - from: "README.md"
      to: "package.json name"
      via: "npx command matches package name"
      pattern: "npx milieu-cli"
---

<objective>
Finalize package metadata, rewrite README for the current architecture, add bin alias, and verify npx works from tarball.

Purpose: The package.json is missing npm metadata (keywords, repository, homepage, bugs, author) that surfaces on npmjs.com. The README describes the old scoring system ("Schema Markup 6/10") and references `npx milieu-content-score` instead of `npx milieu-cli`. A `milieu-cli` bin alias is needed for belt-and-suspenders npx compatibility. This plan makes the package publish-ready.

Output: Updated `package.json` with full metadata + bin alias, rewritten `README.md`, verified npx smoke test.
</objective>

<execution_context>
@/Users/arun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/arun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-packaging-and-publishing/10-RESEARCH.md
@.planning/phases/10-packaging-and-publishing/10-01-SUMMARY.md

<interfaces>
<!-- From package.json (current state before this plan) -->
```json
{
  "name": "milieu-cli",
  "version": "0.1.0",
  "description": "Measure how legible your product is to AI agents",
  "bin": {
    "milieu": "./dist/cli/index.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  }
}
```

Git remote: https://github.com/simplyarun/milieu-content-score-cli.git
License: Apache-2.0
Node engine: >=18
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update package.json metadata and bin alias, rewrite README.md</name>
  <files>package.json, README.md</files>
  <read_first>
    - package.json (current full contents)
    - README.md (current stale contents to replace)
    - src/render/format-scan.ts (to understand current output format for README examples)
    - src/render/format-bridge.ts (bridge formatting for README output example)
  </read_first>
  <action>
1. Update `package.json` with these additions/changes:

Add `milieu-cli` bin alias (belt-and-suspenders for npx):
```json
"bin": {
  "milieu": "./dist/cli/index.js",
  "milieu-cli": "./dist/cli/index.js"
}
```

Add npm metadata fields:
```json
"keywords": [
  "ai",
  "ai-agents",
  "llm",
  "seo",
  "cli",
  "scanner",
  "robots-txt",
  "llms-txt",
  "openapi",
  "schema-org",
  "content-score",
  "web-standards"
],
"repository": {
  "type": "git",
  "url": "https://github.com/simplyarun/milieu-content-score-cli.git"
},
"homepage": "https://github.com/simplyarun/milieu-content-score-cli#readme",
"bugs": {
  "url": "https://github.com/simplyarun/milieu-content-score-cli/issues"
},
"author": "Arun"
```

Do NOT change: name, version, description, type, main, types, exports, files, engines, license, dependencies, devDependencies. Only ADD the new fields and update bin.

2. Rewrite `README.md` to describe the current 5-bridge architecture. Structure:

```markdown
# milieu-cli

Measure how legible your product is to AI agents. A Lighthouse for the agentic web.

Runs deterministic checks across 5 bridges of machine legibility against any public URL. No AI, no API keys, no config. Same site, same score, every time.

## Quick start

\`\`\`bash
npx milieu-cli scan stripe.com
\`\`\`

After install:

\`\`\`bash
milieu scan stripe.com
\`\`\`

## What it checks

| Bridge | What it measures | Score |
|--------|------------------|-------|
| **Bridge 1: Reachability** | HTTPS, HTTP status, robots.txt, AI crawler policies, meta/header robots | 0-100 |
| **Bridge 2: Standards** | OpenAPI, llms.txt, MCP endpoint, JSON-LD, Schema.org, security.txt, ai-plugin.json | 0-100 |
| **Bridge 3: Separation** | API presence, developer docs, SDK references, webhook support | Detection inventory |
| **Bridge 4: Schema** | Not yet evaluated | -- |
| **Bridge 5: Context** | Not yet evaluated | -- |

## Options

| Flag | Description | Default |
|------|-------------|---------|
| `--json` | Output raw JSON | off |
| `--pretty` | Pretty-print JSON (use with --json) | off |
| `--verbose` | Show individual check details | off |
| `--timeout <ms>` | Per-request timeout in milliseconds | 10000 |
| `--threshold <n>` | Exit non-zero if overall score < n | off |
| `--quiet` | Suppress terminal output | off |

## JSON output

\`\`\`bash
milieu scan stripe.com --json --pretty
\`\`\`

Returns the complete `ScanResult` object. JSON output is a versioned public API surface.

## Programmatic API

\`\`\`typescript
import { scan } from "milieu-cli";

const result = await scan("https://stripe.com", { timeout: 15000 });
console.log(result.score);       // number
console.log(result.bridges);     // [BridgeResult, BridgeResult, BridgeResult, BridgeResult, BridgeResult]
\`\`\`

All types are exported:

\`\`\`typescript
import type { ScanResult, BridgeResult, Check, CheckStatus } from "milieu-cli";
\`\`\`

## Requirements

- Node.js 18+

## License

Apache-2.0
```

Key changes from old README:
- Package name: `milieu-content-score` -> `milieu-cli`
- Architecture: 5 named checks -> 5 bridges of machine legibility
- Scoring: points-based (70/100) -> bridge-level progress bars
- CLI syntax: `npx milieu-content-score` -> `npx milieu-cli scan`
- Added: programmatic API section, options table, JSON output
  </action>
  <verify>
    <automated>cd /Users/arun/Milieu-Dev/milieu-content-score-cli && node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); const ok = p.bin['milieu-cli'] && p.keywords && p.repository && p.homepage; if(!ok) process.exit(1); console.log('metadata OK');"</automated>
  </verify>
  <acceptance_criteria>
    - package.json has bin.milieu-cli pointing to ./dist/cli/index.js
    - package.json has keywords array with 12 terms
    - package.json has repository, homepage, bugs, author fields
    - README.md references "milieu-cli" not "milieu-content-score"
    - README.md describes 5-bridge architecture
    - README.md shows `npx milieu-cli scan` as primary command
    - README.md includes programmatic API section
  </acceptance_criteria>
  <done>Package metadata is complete for npm listing. README accurately describes the current architecture and CLI syntax.</done>
</task>

<task type="auto">
  <name>Task 2: Verify npx smoke test from tarball</name>
  <files></files>
  <read_first>
    - package.json (to confirm bin entries)
    - dist/cli/index.js (first few lines for shebang)
  </read_first>
  <action>
1. Run `npm run build` to ensure dist is current.

2. Run `npm pack` to create the tarball (e.g., `milieu-cli-0.1.0.tgz`).

3. Verify tarball contents:
   - `npm pack --dry-run` shows zero `__tests__` entries
   - File count is under 100
   - README.md is included (npm includes it automatically)
   - package.json is included

4. Test npx from the local tarball:
   ```bash
   npx ./milieu-cli-0.1.0.tgz scan https://example.com
   ```
   This simulates what a first-time npx user experiences. It should:
   - Resolve the single bin entry
   - Execute with the `scan` subcommand
   - Produce bridge output (or a network-dependent result)

5. Verify the programmatic API entry point resolves:
   ```bash
   node -e "import('milieu-cli').then(m => console.log(typeof m.scan))"
   ```
   This should print "function" (requires being in the package directory or having it installed).

6. Clean up the tarball after testing:
   ```bash
   rm milieu-cli-*.tgz
   ```

7. Run the full test suite to confirm no regressions:
   ```bash
   npx vitest run --reporter=verbose
   ```
  </action>
  <verify>
    <automated>cd /Users/arun/Milieu-Dev/milieu-content-score-cli && npm run build && npm pack --dry-run 2>&1 | grep "total files"</automated>
  </verify>
  <acceptance_criteria>
    - npm pack creates a tarball successfully
    - npm pack --dry-run shows under 100 total files
    - npm pack --dry-run shows 0 occurrences of __tests__
    - npx from tarball runs without module resolution errors
    - Full test suite passes
  </acceptance_criteria>
  <done>Package tarball is verified: correct file count, no test artifacts, npx execution works. Package is ready for npm publish.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify package is ready for npm publish</name>
  <files></files>
  <action>
Human verification checkpoint. Claude has automated everything above -- this is the final review before publishing.

What was built:
- Clean build pipeline excluding test files from dist (tsconfig.build.json)
- Package.json metadata (keywords, repository, homepage, bugs, author, bin alias)
- README rewritten for current 5-bridge architecture
- Automated packaging tests (8 assertions)
- npx smoke test from local tarball verified
  </action>
  <verify>
    <automated>cd /Users/arun/Milieu-Dev/milieu-content-score-cli && npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - Human has reviewed README.md content
    - Human has reviewed package.json metadata (author, repo URL)
    - Human has confirmed npm pack output looks correct
    - Human approves for npm publish
  </acceptance_criteria>
  <done>Human approves package for npm publish.</done>
  <how-to-verify>
    1. Review the README.md -- does it accurately describe the tool?
    2. Review package.json metadata -- correct author, repository URL, keywords?
    3. Run `npm pack --dry-run` -- file count under 100? No test files?
    4. Optionally test: `npm pack && npx ./milieu-cli-0.1.0.tgz scan example.com`
    5. If satisfied, run `npm publish` to publish to npm (requires npm login).
    6. After publish, test: `npx milieu-cli scan example.com` from a clean directory.
  </how-to-verify>
  <resume-signal>Type "approved" to confirm package is publish-ready, or describe any issues with the packaging.</resume-signal>
</task>

</tasks>

<verification>
1. `npm pack --dry-run` shows under 100 files and zero __tests__ entries
2. `npx ./milieu-cli-*.tgz scan https://example.com` produces output
3. `npx vitest run --reporter=verbose` -- full suite passes
4. package.json has all metadata fields: keywords, repository, homepage, bugs, author, bin.milieu-cli
5. README.md references milieu-cli, 5 bridges, correct CLI syntax
</verification>

<success_criteria>
- Package tarball contains only production files (under 100 files, zero test artifacts)
- npx execution from tarball works without errors
- npm metadata is complete for professional listing on npmjs.com
- README accurately describes the 5-bridge architecture and `milieu scan` command
- Human has verified and approved the package for publish
</success_criteria>

<output>
After completion, create `.planning/phases/10-packaging-and-publishing/10-02-SUMMARY.md`
</output>
