---
phase: 01-project-scaffold-and-types
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - .gitignore
  - src/bridges/index.ts
  - src/core/index.ts
  - src/render/index.ts
  - src/utils/index.ts
  - src/cli/index.ts
  - src/index.ts
autonomous: true
requirements:
  - FOUND-02

must_haves:
  truths:
    - "Old source code (src/checks/, src/scoring.ts, src/types.ts, src/index.ts) is deleted"
    - "New directory structure exists with barrel exports"
    - "package.json identifies the project as milieu-cli at version 0.1.0"
    - "TypeScript compiles with zero errors on the empty scaffold"
  artifacts:
    - path: "package.json"
      provides: "Project identity and build scripts"
      contains: "milieu-cli"
    - path: "tsconfig.json"
      provides: "TypeScript compiler configuration"
      contains: "ESNext"
    - path: "src/bridges/index.ts"
      provides: "Barrel export for bridge modules"
    - path: "src/core/index.ts"
      provides: "Barrel export for core types and logic"
    - path: "src/render/index.ts"
      provides: "Barrel export for rendering modules"
    - path: "src/utils/index.ts"
      provides: "Barrel export for utility modules"
    - path: "src/cli/index.ts"
      provides: "Barrel export for CLI modules"
    - path: "src/index.ts"
      provides: "Package entry point re-exporting all barrels"
  key_links:
    - from: "src/index.ts"
      to: "src/core/index.ts"
      via: "re-export"
      pattern: "export.*from.*./core"
---

<objective>
Gut the existing codebase and establish the new milieu-cli project scaffold.

Purpose: Every subsequent phase needs a clean directory structure and working build pipeline. This plan removes old code, rewrites config files from scratch, and creates the directory skeleton with barrel exports.
Output: Empty but compilable project scaffold with new identity (milieu-cli 0.1.0).
</objective>

<execution_context>
@/Users/arun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/arun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-scaffold-and-types/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove old source code and rewrite project configs</name>
  <files>package.json, tsconfig.json, .gitignore</files>
  <read_first>
    - package.json (current state to understand what exists)
    - tsconfig.json (current state)
    - .planning/phases/01-project-scaffold-and-types/01-CONTEXT.md (locked decisions)
  </read_first>
  <action>
1. Delete all files inside src/ directory (src/checks/, src/index.ts, src/scoring.ts, src/types.ts).

2. Write package.json from scratch with these exact values:
```json
{
  "name": "milieu-cli",
  "version": "0.1.0",
  "description": "Measure how legible your product is to AI agents",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "milieu": "./dist/cli/index.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4",
    "typescript": "^5.9"
  }
}
```

Note: tsup is removed -- using tsc directly for build (per Claude's discretion in CONTEXT.md). @types/node version pinned to ^22.0.0 for Node 18 compatibility. License stays Apache-2.0 per locked decision. Three runtime deps (commander, chalk, ora) will be added in later phases when needed.

3. Write tsconfig.json from scratch:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "outDir": "dist",
    "rootDir": "src",
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "resolveJsonModule": true,
    "isolatedModules": true
  },
  "include": ["src"],
  "exclude": ["node_modules", "dist"]
}
```

Note: Using NodeNext module/moduleResolution (not bundler) since we build with tsc directly and target ESM. This requires .js extensions on imports which aligns with existing conventions.

4. Write .gitignore:
```
node_modules/
dist/
*.tsbuildinfo
.DS_Store
```
  </action>
  <verify>
    <automated>ls src/ 2>&1 | grep -v "checks\|scoring\|types.ts" | head -5 && grep '"milieu-cli"' package.json && grep '"NodeNext"' tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"name": "milieu-cli"`
    - package.json contains `"version": "0.1.0"`
    - package.json contains `"license": "Apache-2.0"`
    - package.json contains `"type": "module"`
    - package.json does NOT contain `tsup`
    - tsconfig.json contains `"module": "NodeNext"`
    - tsconfig.json contains `"declaration": true`
    - .gitignore contains `dist/`
    - src/checks/ directory does NOT exist
    - src/scoring.ts does NOT exist
    - src/types.ts does NOT exist (old types file removed)
  </acceptance_criteria>
  <done>Old code is gone, project identifies as milieu-cli 0.1.0 with Apache-2.0, build config targets ESM via tsc</done>
</task>

<task type="auto">
  <name>Task 2: Create directory structure with barrel exports</name>
  <files>src/bridges/index.ts, src/core/index.ts, src/render/index.ts, src/utils/index.ts, src/cli/index.ts, src/index.ts</files>
  <read_first>
    - package.json (verify main/types/exports fields point to correct paths)
    - tsconfig.json (verify rootDir is src, outDir is dist)
  </read_first>
  <action>
1. Create directory structure:
   - src/bridges/
   - src/core/
   - src/render/
   - src/utils/
   - src/cli/

2. Create barrel export files -- each starts as an empty named export so TypeScript compiles without error and future phases have clear import targets:

src/bridges/index.ts:
```typescript
// Bridge check implementations (Phases 3-5)
export {};
```

src/core/index.ts:
```typescript
// Core types and scoring logic (Phases 1-2, 5)
export {};
```

src/render/index.ts:
```typescript
// Terminal rendering and output formatting (Phase 6)
export {};
```

src/utils/index.ts:
```typescript
// HTTP client, URL normalization, SSRF protection (Phase 2)
export {};
```

src/cli/index.ts:
```typescript
// CLI entry point and command handling (Phase 7)
export {};
```

3. Create src/index.ts as the package entry point that re-exports from all barrels:
```typescript
// milieu-cli - Measure how legible your product is to AI agents
// Package entry point: re-exports public API

export * from "./core/index.js";
```

Note: Only re-export core for now (types). Other barrels (bridges, render, utils, cli) are internal implementation details not exposed as public API. The cli barrel is used via the bin entry point, not via library import.

4. Run `npm install` to regenerate package-lock.json for the new package.json.

5. Run `npx tsc --noEmit` to verify the scaffold compiles with zero errors.
  </action>
  <verify>
    <automated>cd /Users/arun/Milieu-Dev/milieu-content-score-cli && npx tsc --noEmit 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - src/bridges/index.ts exists and contains `export {}`
    - src/core/index.ts exists and contains `export {}`
    - src/render/index.ts exists and contains `export {}`
    - src/utils/index.ts exists and contains `export {}`
    - src/cli/index.ts exists and contains `export {}`
    - src/index.ts exists and contains `export * from "./core/index.js"`
    - `npx tsc --noEmit` exits with code 0 (zero TypeScript errors)
  </acceptance_criteria>
  <done>Five directory barrels created, package entry re-exports core, TypeScript compiles cleanly</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0 (scaffold compiles)
- `grep -r "milieu-content-score" package.json` returns nothing (old name gone)
- `ls src/checks/ 2>&1` shows "No such file or directory" (old code removed)
- `ls src/bridges/index.ts src/core/index.ts src/render/index.ts src/utils/index.ts src/cli/index.ts` all exist
</verification>

<success_criteria>
- Project identifies as milieu-cli 0.1.0 with Apache-2.0 license
- Old source code is completely removed
- Five subdirectories exist under src/ with barrel exports
- TypeScript compiles with zero errors
- Build pipeline uses tsc (not tsup)
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-scaffold-and-types/01-01-SUMMARY.md`
</output>
