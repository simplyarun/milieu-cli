---
phase: 06-terminal-rendering-and-output-stubs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bridges/stubs.ts
  - src/bridges/__tests__/stubs.test.ts
  - src/bridges/index.ts
  - src/render/colors.ts
  - src/render/progress-bar.ts
  - src/render/symbols.ts
  - src/render/__tests__/colors.test.ts
  - src/render/__tests__/progress-bar.test.ts
  - src/render/index.ts
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - STUB-01
  - STUB-02
  - STUB-03
  - TERM-02
  - TERM-05
must_haves:
  truths:
    - "Bridge 4 stub returns BridgeResult with id=4, name='Schema', status='not_evaluated', score=null, scoreLabel=null, checks=[], durationMs=0"
    - "Bridge 5 stub returns BridgeResult with id=5, name='Context', status='not_evaluated', score=null, scoreLabel=null, checks=[], durationMs=0"
    - "Stub messages contain no 'coming soon', 'upgrade', 'future', or 'planned' language"
    - "Color functions return plain text when NO_COLOR is set and non-empty"
    - "Color functions return chalk-styled text when NO_COLOR is not set"
    - "Progress bar renders 12 characters with correct color for score range"
  artifacts:
    - path: "src/bridges/stubs.ts"
      provides: "Bridge 4 and 5 factory functions"
      exports: ["createBridge4Stub", "createBridge5Stub"]
    - path: "src/render/colors.ts"
      provides: "Centralized chalk wrapper with NO_COLOR support"
      exports: ["green", "yellow", "red", "cyan", "dim", "bold"]
    - path: "src/render/progress-bar.ts"
      provides: "12-char progress bar generator"
      exports: ["progressBar"]
    - path: "src/render/symbols.ts"
      provides: "Status indicator symbols for verbose mode"
      exports: ["statusSymbol"]
    - path: "src/bridges/__tests__/stubs.test.ts"
      provides: "Tests for bridge stubs"
    - path: "src/render/__tests__/colors.test.ts"
      provides: "Tests for color utility and NO_COLOR"
    - path: "src/render/__tests__/progress-bar.test.ts"
      provides: "Tests for progress bar rendering"
  key_links:
    - from: "src/render/colors.ts"
      to: "chalk"
      via: "import chalk from 'chalk'"
      pattern: "import chalk from .chalk."
    - from: "src/render/progress-bar.ts"
      to: "src/render/colors.ts"
      via: "import { green, yellow, red } from './colors.js'"
      pattern: "import.*from.*colors"
    - from: "src/bridges/stubs.ts"
      to: "src/core/types.ts"
      via: "import type { BridgeResult } from '../core/types.js'"
      pattern: "import type.*BridgeResult"
---

<objective>
Build the foundational rendering building blocks and Bridge 4-5 stubs.

Purpose: Establishes the color system (with NO_COLOR support), progress bar, status symbols, and bridge stub factories that Plan 02 composes into full scan output formatting.
Output: chalk/ora installed, 6 source files, 3 test files, all tests green.
</objective>

<execution_context>
@/Users/arun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/arun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-terminal-rendering-and-output-stubs/06-RESEARCH.md

@src/core/types.ts
@src/bridges/index.ts
@src/render/index.ts

<interfaces>
<!-- Key types the executor needs from the existing codebase -->

From src/core/types.ts:
```typescript
export type BridgeId = 1 | 2 | 3 | 4 | 5;
export type BridgeName = "Reachability" | "Standards" | "Separation" | "Schema" | "Context";
export type BridgeStatus = "evaluated" | "not_evaluated";
export type CheckStatus = "pass" | "partial" | "fail" | "error";

export interface BridgeResult {
  id: BridgeId;
  name: BridgeName;
  status: BridgeStatus;
  score: number | null;
  scoreLabel: "pass" | "partial" | "fail" | null;
  checks: Check[];
  durationMs: number;
  message?: string;
  abort?: boolean;
  abortReason?: string;
}
```

From src/bridges/index.ts (current):
```typescript
export { runReachabilityBridge } from "./reachability/index.js";
export { runStandardsBridge } from "./standards/index.js";
export { runSeparationBridge } from "./separation/index.js";
```

From src/render/index.ts (current):
```typescript
export {};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bridge 4-5 stub factories and tests</name>
  <files>src/bridges/stubs.ts, src/bridges/__tests__/stubs.test.ts, src/bridges/index.ts</files>
  <read_first>src/core/types.ts, src/bridges/index.ts, src/bridges/separation/__tests__/api-presence.test.ts</read_first>
  <behavior>
    - createBridge4Stub() returns BridgeResult with id=4, name="Schema", status="not_evaluated", score=null, scoreLabel=null, checks=[], durationMs=0, message="Schema quality assessment requires deeper analysis beyond automated checks."
    - createBridge5Stub() returns BridgeResult with id=5, name="Context", status="not_evaluated", score=null, scoreLabel=null, checks=[], durationMs=0, message="Context evaluation requires deeper analysis beyond automated checks."
    - Neither stub message contains "coming soon", "upgrade", "future", "planned", "premium", or "later"
  </behavior>
  <action>
1. Create `src/bridges/__tests__/stubs.test.ts` with these tests (RED phase):
   - Test createBridge4Stub returns correct BridgeResult shape: id=4, name="Schema", status="not_evaluated", score=null, scoreLabel=null, checks=[] (empty array), durationMs=0
   - Test createBridge4Stub message equals exactly: "Schema quality assessment requires deeper analysis beyond automated checks."
   - Test createBridge5Stub returns correct BridgeResult shape: id=5, name="Context", status="not_evaluated", score=null, scoreLabel=null, checks=[] (empty array), durationMs=0
   - Test createBridge5Stub message equals exactly: "Context evaluation requires deeper analysis beyond automated checks."
   - Test STUB-03: For both stubs, assert message does NOT match /coming soon|upgrade|future|planned|premium|later/i

2. Create `src/bridges/stubs.ts` (GREEN phase):
   ```typescript
   import type { BridgeResult } from "../core/types.js";

   export function createBridge4Stub(): BridgeResult {
     return {
       id: 4,
       name: "Schema",
       status: "not_evaluated",
       score: null,
       scoreLabel: null,
       checks: [],
       durationMs: 0,
       message: "Schema quality assessment requires deeper analysis beyond automated checks.",
     };
   }

   export function createBridge5Stub(): BridgeResult {
     return {
       id: 5,
       name: "Context",
       status: "not_evaluated",
       score: null,
       scoreLabel: null,
       checks: [],
       durationMs: 0,
       message: "Context evaluation requires deeper analysis beyond automated checks.",
     };
   }
   ```

3. Update `src/bridges/index.ts` to add:
   ```typescript
   export { createBridge4Stub, createBridge5Stub } from "./stubs.js";
   ```
   Keep the existing 3 bridge exports. Add the stubs export after them.
  </action>
  <verify>
    <automated>npx vitest run src/bridges/__tests__/stubs.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'createBridge4Stub' src/bridges/stubs.ts returns 0
    - grep -q 'createBridge5Stub' src/bridges/stubs.ts returns 0
    - grep -q '"Schema quality assessment requires deeper analysis beyond automated checks."' src/bridges/stubs.ts returns 0
    - grep -q '"Context evaluation requires deeper analysis beyond automated checks."' src/bridges/stubs.ts returns 0
    - grep -q 'createBridge4Stub' src/bridges/index.ts returns 0
    - grep -q 'createBridge5Stub' src/bridges/index.ts returns 0
    - npx vitest run src/bridges/__tests__/stubs.test.ts exits 0
  </acceptance_criteria>
  <done>Both stub factories return exact BridgeResult shapes per spec, no forbidden language in messages, stubs re-exported from bridges barrel, all tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Color utility, progress bar, symbols, and tests</name>
  <files>src/render/colors.ts, src/render/progress-bar.ts, src/render/symbols.ts, src/render/__tests__/colors.test.ts, src/render/__tests__/progress-bar.test.ts, src/render/index.ts, package.json</files>
  <read_first>src/core/types.ts, src/render/index.ts, package.json, .planning/phases/06-terminal-rendering-and-output-stubs/06-RESEARCH.md</read_first>
  <behavior>
    - colors.ts: green/yellow/red/cyan/dim/bold functions return chalk-styled strings by default
    - colors.ts: When NO_COLOR is set and non-empty, all color functions return input string unchanged (no ANSI codes)
    - colors.ts: NO_COLOR takes precedence over FORCE_COLOR
    - progress-bar.ts: progressBar(100) returns 12 filled characters colored green
    - progress-bar.ts: progressBar(0) returns 12 empty characters colored red
    - progress-bar.ts: progressBar(50) returns 6 filled + 6 empty characters colored yellow
    - progress-bar.ts: progressBar(85) returns ~10 filled + ~2 empty characters colored green (score >= 80)
    - progress-bar.ts: progressBar(39) returns colored red (score < 40)
    - symbols.ts: statusSymbol("pass") returns green checkmark, statusSymbol("fail") returns red x, statusSymbol("partial") returns yellow warning, statusSymbol("error") returns red x
  </behavior>
  <action>
1. Install chalk and ora as production dependencies:
   ```bash
   npm install chalk@5 ora@9
   ```
   This adds exactly 2 production deps (commander added in Phase 7 completes the 3-dep target per PKG-03).

2. Create `src/render/__tests__/colors.test.ts` (RED phase):
   - Test: green("hello") returns string containing ANSI escape codes (when NO_COLOR is not set)
   - Test: yellow("hello") returns string containing ANSI escape codes
   - Test: red("hello") returns string containing ANSI escape codes
   - Test: cyan("hello") returns string containing ANSI escape codes
   - Test: dim("hello") returns string containing ANSI escape codes
   - Test: bold("hello") returns string containing ANSI escape codes
   - Test: All functions return strings containing "hello" (the input text is preserved)
   - Test NO_COLOR: Use `vi.stubEnv('NO_COLOR', '1')` before importing a fresh module (use `vi.resetModules()` + dynamic import). All 6 functions must return EXACTLY the input string with zero ANSI codes.
   - Test NO_COLOR empty string: `NO_COLOR=""` should NOT disable colors (spec says "present and non-empty")

   IMPORTANT for NO_COLOR tests: colors.ts evaluates `isColorEnabled()` at module load time. To test with different env values, use vitest `vi.resetModules()` and dynamic `import()` to get a fresh module evaluation. Structure:
   ```typescript
   describe("with NO_COLOR=1", () => {
     beforeEach(() => {
       vi.stubEnv("NO_COLOR", "1");
       vi.resetModules();
     });
     afterEach(() => {
       vi.unstubAllEnvs();
     });
     it("green returns plain text", async () => {
       const { green } = await import("../colors.js");
       expect(green("hello")).toBe("hello");
     });
   });
   ```

3. Create `src/render/colors.ts` (GREEN phase):
   ```typescript
   import chalk from "chalk";

   function isColorEnabled(): boolean {
     const noColor = process.env["NO_COLOR"];
     if (noColor !== undefined && noColor !== "") {
       return false;
     }
     return true;
   }

   const colorEnabled = isColorEnabled();

   export function green(text: string): string {
     return colorEnabled ? chalk.green(text) : text;
   }
   export function yellow(text: string): string {
     return colorEnabled ? chalk.yellow(text) : text;
   }
   export function red(text: string): string {
     return colorEnabled ? chalk.red(text) : text;
   }
   export function cyan(text: string): string {
     return colorEnabled ? chalk.cyan(text) : text;
   }
   export function dim(text: string): string {
     return colorEnabled ? chalk.dim(text) : text;
   }
   export function bold(text: string): string {
     return colorEnabled ? chalk.bold(text) : text;
   }
   ```

4. Create `src/render/__tests__/progress-bar.test.ts` (RED phase):
   - Test: progressBar(100) returns exactly 12 characters of content (ignoring ANSI codes). Use strip-ansi approach: match /[\u001b\u009b][[()#;?]*(?:[0-9]{1,4}(?:;[0-9]{0,4})*)?[0-9A-ORZcf-nqry=><]/g to strip ANSI, then check visible length is 12.
   - Test: progressBar(0) returns 12 characters (all empty markers)
   - Test: progressBar(50) returns 6 filled + 6 empty markers
   - Test: progressBar(85) uses green coloring (score >= 80). Verify by importing green from colors.ts and checking progressBar(85) contains the same ANSI prefix as green("x").
   - Test: progressBar(50) uses yellow coloring (40 <= score < 80)
   - Test: progressBar(20) uses red coloring (score < 40)
   - Test: progressBar(80) uses green (boundary: >= 80 is green)
   - Test: progressBar(40) uses yellow (boundary: >= 40 is yellow)
   - Test: progressBar(39) uses red (boundary: < 40 is red)

5. Create `src/render/progress-bar.ts` (GREEN phase):
   ```typescript
   import { green, yellow, red } from "./colors.js";

   /**
    * Render a 12-character progress bar for a scored bridge.
    * Score 0-100 maps to 0-12 filled characters.
    *
    * Uses Unicode block characters:
    * - U+2588 (full block) for filled
    * - U+2591 (light shade) for empty
    */
   export function progressBar(score: number): string {
     const width = 12;
     const filled = Math.round((score / 100) * width);
     const empty = width - filled;
     const bar = "\u2588".repeat(filled) + "\u2591".repeat(empty);

     if (score >= 80) return green(bar);
     if (score >= 40) return yellow(bar);
     return red(bar);
   }
   ```

6. Create `src/render/symbols.ts`:
   ```typescript
   import { green, red, yellow, dim } from "./colors.js";

   /**
    * Return a colored status symbol for verbose check display.
    *
    * pass    -> green checkmark
    * partial -> yellow warning
    * fail    -> red x
    * error   -> red x
    */
   export function statusSymbol(status: "pass" | "partial" | "fail" | "error"): string {
     switch (status) {
       case "pass":
         return green("\u2714");
       case "partial":
         return yellow("\u26A0");
       case "fail":
       case "error":
         return red("\u2718");
     }
   }
   ```

7. Update `src/render/index.ts` to replace the empty export:
   ```typescript
   // Terminal rendering and output formatting (Phase 6)
   export { green, yellow, red, cyan, dim, bold } from "./colors.js";
   export { progressBar } from "./progress-bar.js";
   export { statusSymbol } from "./symbols.js";
   ```
  </action>
  <verify>
    <automated>npx vitest run src/render/__tests__/colors.test.ts src/render/__tests__/progress-bar.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - grep -q '"chalk"' package.json returns 0 (chalk is a dependency)
    - grep -q '"ora"' package.json returns 0 (ora is a dependency)
    - grep -q 'isColorEnabled' src/render/colors.ts returns 0
    - grep -q 'NO_COLOR' src/render/colors.ts returns 0
    - grep -q 'export function green' src/render/colors.ts returns 0
    - grep -q 'export function progressBar' src/render/progress-bar.ts returns 0
    - grep -q 'export function statusSymbol' src/render/symbols.ts returns 0
    - grep -q 'progressBar' src/render/index.ts returns 0
    - grep -q 'statusSymbol' src/render/index.ts returns 0
    - npx vitest run src/render/__tests__/colors.test.ts src/render/__tests__/progress-bar.test.ts exits 0
  </acceptance_criteria>
  <done>chalk and ora installed as production deps. Color utility wraps chalk with NO_COLOR support. Progress bar renders 12-char bars with score-based coloring. Symbols module provides status indicators. All tests green. Render barrel re-exports all public API.</done>
</task>

</tasks>

<verification>
npx vitest run src/bridges/__tests__/stubs.test.ts src/render/__tests__/colors.test.ts src/render/__tests__/progress-bar.test.ts --reporter=verbose
npx tsc --noEmit
</verification>

<success_criteria>
- Bridge 4 and 5 stubs return exact BridgeResult shapes with correct messages
- No forbidden language in stub messages (STUB-03)
- All 6 color functions delegate to chalk and respect NO_COLOR
- Progress bar produces 12-char output with correct color thresholds (green >= 80, yellow >= 40, red < 40)
- Status symbols return correct colored Unicode characters
- chalk and ora are production dependencies in package.json
- All new tests pass, typecheck clean
</success_criteria>

<output>
After completion, create `.planning/phases/06-terminal-rendering-and-output-stubs/06-01-SUMMARY.md`
</output>
